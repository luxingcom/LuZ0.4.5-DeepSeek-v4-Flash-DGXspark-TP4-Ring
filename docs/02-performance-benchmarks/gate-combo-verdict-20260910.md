# 参数组合 · 门禁通过判定表（batch × k）

**签发**：team-lead（工程保障团队督导）
**日期**：2026-09-10
**基准口径**：**最近一次同条件实测**（09-09 23:4x – 09-10 00:3x 跨日窗口，同一栈 / 同一 EP=8002 直连 / 同一 run_cell 载荷）
**数据源**：`matrix-batch-k-ab-20260909.md`（09-10 00:30 定稿）+ `rex_head_k3boot.txt` / `rex_k3_failstate.txt` / `rex_final_state.txt`（09-10 00:16–00:28 实测证据）

---

## 🆕 附：G4 双臂验收（S1 补丁 A/B）· OLD 臂实测已出（09-10 追加）

> 本节与上表「batch × k 参数组合」**不同议题** —— 上表选**参数档**，本节验 **S1 补丁有无**。
> 两臂唯一差异 = 镜像（OLD = `...TP4-Ring` / NEW = `...TP4-Ring-VL-S1-20260910b`），参数档固定 `bat4096 × k6 × thr0`。

### OLD 臂 DE_coding_C1 ×5（原始值，09-10 实测）

| 轮 | decode_p50 | prefill_p50 | ttft_p50 | ok |
|---|---|---|---|---|
| r1 | 78.81 | 1677.01 | 0.3518 | 1 |
| r2 | 86.05 | 1765.08 | 0.3309 | 1 |
| r3 | 82.73 | 1786.50 | 0.3275 | 1 |
| r4 | 81.51 | 1785.88 | 0.3337 | 1 |
| r5 | 80.40 | 1807.81 | 0.3330 | 1 |

**统计量（督导独立复算，6/6 逐位命中）**：mean=`81.9000` / median=`81.5100` / stdev=`2.7323` / min=`78.81` / max=`86.05` / range=`7.2400` / MAD=`1.2200` / **4×MAD=`4.8800`（= OLD 臂内噪声地板 F_OLD）**
- **CI95 半宽（两臂对比用）= T_DF8(2.306) × sp(2.7323) × √0.4 = `3.9849`**
- seed：warmup=999901；r1..r5 = 83000/84000/85000/86000/87000

**值守四件套（全程零异常）**：StartedAt 全程未变 `2026-09-10T12:34:22.710843376Z`（⇒ **无重建**）／TAG 全程 OLD 未漂移／health 全程 200／journalctl「healthy 无需干预」=36、「触发主动重建」=**0**。

**★ 关键机制验证**：`check_idle` [C6-fix2] **实锤生效** —— 5 轮每轮读到 `num_requests_running=0.0` 均**正确放行**（旧版 `[ "$R" != "0" ]` 必假 ABORT）。**该缺陷闭环。**

**落地证据**：`~/w6-kit/rex_g4_handoff.sh` md5=`5cfaa7943a333e00be42772d0cfb2282`（LF 11476B，服务器侧校验）／**全新安装（NO_PRIOR）非覆盖 ⇒ 无 .bak**

### NEW 臂：**进行中**（切臂中）
- 前置 #1 **PASS**：四机 NEW RepoDigest 一致 = `sha256:<BAKE_IMAGE_DIGEST>`（IMAGE ID 跨存储驱动双口径系已知特性，非故障）
- 待完成：`set_tag` md5 断言 → `restart_stack` → `e0_identity NEW` → warmup×2 → 5 轮 → tokenids×3 → `step4_stats`

### step4_stats 判定框架（两臂齐后执行）
- **F 地板**：`F = 4.0 × MAD(OLD) = 4.8800`（臂内噪声上界）
- **CI95**：`delta ± T_DF8(2.306) × sp × √(1/5+1/5)`
- **双向判据**：`|Δ| ≤ F` → PASS ／ 方向符合机制 → 记录 ／ 方向相反 → FAIL ／ **CI95 跨 0 或跨 F → 证据不足**
- **G-E 效率门禁**：四格（code/json × 两方向）相对基线 **≥ +8%**，且不得出现 **< −1.1%** 的逆向格
- ⚠️ **F 含"载荷分量"**：`--uuid-prefix` 令每轮载荷不同 ⇒ F 偏保守（上界）

---

## 📌 TL;DR

**唯一通过全部门禁的组合 = `batch=4096 × k=6 × thr=0`（即现役锁定态）。**

其余组合判定：
- `batch=4608`：**可启动（硬门禁通过）但效率门禁不通过** → 不晋升
- `batch=8192`：**硬门禁直接拒绝**（引擎层硬上限） → 不可行
- `k=3` / `k=4`：**硬门禁拒绝**（DSpark validator） → 不可行
- `k=5`：**硬门禁通过但收益门禁不通过**（0~+6% < +8%） → 本期不排窗口

---

## 🎯 门禁框架（三道门，顺序短路）

| 门 | 名称 | 判据 | 未过后果 |
|---|---|---|---|
| **G-H** | 硬可行性门 | 引擎能否 boot + health=200 + 四机 healthy | 直接不可行，CLI 不可绕过 |
| **G-E** | 效率门禁 | 四格（code/json）相对基线 **≥ +8%**，且不得出现 **< −1.1%**（噪声带）的逆向格 | 不晋升，维持原档 |
| **G-S** | 稳定门禁 | 无坏读毛刺、KV pool 不得显著挤占、无塌陷通道 | 不晋升 |

> 噪声带 **±1.1%** 为本栈四格重复测量极差上界；晋升门槛 **+8%** 为项目既定线。

---

## 📊 全组合判定矩阵

| # | 组合 | G-H 硬可行性 | G-E 效率 | G-S 稳定 | **总判定** | 依据 |
|---|---|---|---|---|---|---|
| 1 | **bat4096 × k6 × thr0** | ✅ boot 成功、四机 healthy | ✅ 基线本身 | ✅ 无毛刺（thr0 解除塌陷） | ✅ **通过 / 现役** | `rex_final_state.txt`；boot 实锤 `max_num_batched_tokens:4096, num_speculative_tokens:6` |
| 2 | bat4608 × k6 × thr0 | ✅ **可启动**（4608 = b12x 容量档上限） | ❌ 净利 < 噪声带 | ❌ KV 5.28M→4.86M（**−8.0%**） | ❌ **不通过** | 见下方四格表 |
| 3 | bat8192 × k6 × thr0 | ⛔ **拒绝** | — | — | ⛔ **不可行** | `num_tokens (8192) exceeds max_num_tokens (4608)`（rank0 崩溃实证） |
| 4 | bat4096 × k3 | ⛔ **拒绝** | — | — | ⛔ **不可行** | pydantic `DSpark requires num_speculative_tokens >= dspark_block_size (5); got 3`；rank0 `Exited(1)` / health `000` |
| 5 | bat4096 × k4 | ⛔ **拒绝** | — | — | ⛔ **不可行** | 同 validator（block_size=5 硬闸）；Cody 方案 B 需重建镜像 |
| 6 | bat4096 × k5 | ✅ 最小合法档（改 speculative.py 一处 bind-mount 即可） | ❌ 收益 0~+6% | ⚠️ 未实测 | ❌ **本期不排** | `k45-slot-benefit-research`；高并发端才有效，本栈 max-num-seqs=12 |
| 7 | bat4608 × k5 | ⚠️ 未测（两变量叠加，非本期范围） | — | — | ⏸ **未测** | 督导裁决本期只测 k∈{3,6} × 最优 batch |

---

## 🔬 组合 2（bat4608）效率门禁明细 — 唯一"可启动但不通过"的档

**B4608 vs T2（k6+thr0+4096）逐格**：

| 格 | A (k6,4096) | T2 (k6,thr0,4096) | B4608 (k6,thr0,4608) | **B vs T2** | 判读 |
|---|---|---|---|---|---|
| DE_coding_C1 prefill | 1840 | 1792 | 1783 | **−0.5%** | 噪声带内 |
| DE_coding_C1 decode | 81.2 | 82.0 | 84.5 | **+3.0%** | 噪声带边缘 |
| DE_json_C8 decode | 35.4 | 35.7 | 35.7 | **0.0%** | 持平 |
| DE_prose_C1 decode | 47.8 | 48.8 | 48.8 | **0.0%** | 持平 |
| PR2048_C1 prefill | 2748 | 2659 | 2719 | **+2.3%** | 噪声带内 |
| PR2048_C1 TTFT | 0.657 | 0.672 | 0.655 | −2.5% | 持平 |
| *(json_C8 prefill)* | — | 344 | 332 | **−3.5%** | **逆向** |

**门禁裁决**：
- G-E：**无任何一格达到 +8%**；最大正向 +3.0%（decode，噪声带边缘）；出现 **−3.5% 逆向格** → **不通过**
- G-S：4608 预分配更大静态 CUDA graph buffer，**KV pool 5.28M → 4.86M（−8.0%）**，对长并发生效期有害 → **不通过**

---

## 📐 硬上限定谳（组合 3/4/5 的依据）

| 维度 | 硬上限 | 根因 | 可解性 |
|---|---|---|---|
| `max-num-batched-tokens` | **4608**（实为 b12x 向上取整容量档下沿） | flashinfer_b12x CUDA graph 静态容量 `max_num_tokens=4608` | 纯参数不可解；需改引擎 / 重建 |
| `k` 合法值 | **{5, 6, 7+}** | DSpark correctness guard `k >= dspark_block_size(5)` | k=3/4 CLI 不可绕过 |
| 通用 Validator 整除约束 | k 须被 `num_nextn_predict_layers(=3)` 整除 | 仅挡 k=5（5%3≠0） | 可改 `config/speculative.py` 一处 bind-mount 绕过 |

---

## ✅ 行动清单

| # | 行动 | 负责 | 紧急度 |
|---|---|---|---|
| 1 | **维持 `bat4096 × k6 × thr0` 锁定态**（唯一全门禁通过组合） | Rex | P0 |
| 2 | batch 维度**本期关闭**：4608 不晋升、8192 不可行 | — | — |
| 3 | k 维度**本期关闭**：3/4 不可行、5 收益不足；长期走 adaptive verification | lead | P2 |
| 4 | 参数面优化空间收窄 → 转向**置信头 + 自适应验证镜像**（S1） | 全队 | P1 |

---

## ⚠️ 诚实批注

1. **"今天（09-10）没有跑过新的 bat×k 组合实测"**。今日 00:00–19:15 的全部活动为 S0/S1 镜像构建与补丁链路（00:1x–13:1x）+ 设计/审计文档（07:3x–18:4x）。本表所引"同条件实测"= **09-09 23:4x – 09-10 00:3x 跨日窗口的那一轮**（其收口产物 09-10 00:30 落盘），这是距当前最近且唯一同条件可比的数据集。
2. **组合 6（k=5）未实测**——收益 0~+6% 系机制驱动区间（社区同栈 + 机制推导），非本栈直接 A/B。边界值被双 gate 天然挡掉，量化对照稀缺。
3. **组合 7（bat4608 × k5）完全未测**，本表标注"未测"而非"不通过"。
4. 矩阵中间文件 `matrix48_cells.txt`（48 格）mtime 为 **2026-09-02 22:43**，属旧基线，**未用于本表**——时间序纪律：证据文件的 mtime 是证据的一部分。
5. `batch4608-ab-20260909/` 运行目录已被窗口清理，其结论已固化于 `matrix-batch-k-ab-20260909.md`，本表数据取自该定稿文档。

---

> 本判定由工程保障团队产出，关键决策请人类工程负责人复核。

---

## 🆕🆕 附二：G4 双臂验收 · **NEW 臂已完成，终判 PASS**（09-10 14:20 UTC 追加）

> **完整终判报告见 `G4-dualarm-verdict-20260910.md`**（本附录仅列关键结论）

### NEW 臂 DE_coding_C1 ×5（原始值）

| 轮 | decode_p50 | prefill_p50 | ttft_p50 |
|---|---|---|---|
| r1 | 86.25 | 1833.73 | 0.3288 |
| r2 | 80.34 | 1828.65 | 0.3319 |
| r3 | 80.55 | 1800.32 | 0.3266 |
| r4 | 79.16 | 1808.16 | 0.3335 |
| r5 | 85.37 | 1768.86 | 0.3375 |

**统计量**：mean `82.3340` / median `80.5500` / stdev `3.2321` / MAD `1.3900` / 4×MAD `5.5600`

### 两臂终判

| 量 | 值 |
|---|---|
| Δ(mean) | **+0.4340 tps（+0.530%）** |
| F（臂内地板）= 4×MAD(OLD) | **4.8800 tps（5.958%）** |
| CI95(between) | **[−3.9306, +4.7986]**（半宽 4.3646，s_p 2.9926） |
| 判据 | `|Δ| = 0.4340 ≤ F = 4.8800` → **分支 (i) 命中** |

```
VERDICT = PASS（不可区分）
裁定     = 保留 S1b，不回退
```

**支撑**：gate-off 契约等价（`confidence_head=None` ⇒ `_conf_enabled=False` ⇒ 全路径短路）+ `env_gate=UNSET` + `staging_count=0` + 六目标文件 md5 逐位命中 + 值守四件套零异常。

**⚠️ 解读边界**：PASS 语义 = **"gate-off 下不改变生产性能"**，**非** "S1 带来提升"。闸门收益属 G2 开臂任务，本轮未检验。

**负结果（如实记录）**：G4-T1 token 精确比对**原理性不可行** —— 臂内自洽率（OLD 4.0% / NEW 2.0%）**低于** OLD↔NEW 交叉率（7.3%）⇒ 服务器对自身都不可复现。判据改采契约等价。
