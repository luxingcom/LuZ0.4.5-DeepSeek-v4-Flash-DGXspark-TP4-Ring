# vLLM 0.28 升级 · W6 窗口交接包（HANDOFF）

- 交接时点：2026-08-28 08:50（T0=07:37 开窗后 ~73min）
- 交接原因：团队组织方式需重构（需真正启用 EngineeringAssuranceTeam 专家成员与标准工作流，而非通用子代理承载）；主理人亲自动手过多，已由人类负责人纠正
- 本包目的：新会话重开团队后，任何接手人/专家可**零上下文恢复执行**
- 铁律提醒：**生产恢复红线 = T0+7h（15:37 本地时间）前必须完成生产恢复**；剩余窗口充足但不可再损耗

---

## 一、当前现场状态（交接时点实测）

### 1.1 生产（绝对不动，窗口正常停机态）
| 项 | 状态 |
|---|---|
| systemd 单元 |  (node01 管理网末段) vllm-tp4-head=inactive+**disabled**； (node02 管理网末段)/ (node03 管理网末段)/ (node04 管理网末段) vllm-tp4-worker=inactive+**disabled** |
| 生产容器 | 四机 vllm-tp4-rank{0,1,2,3} 已 stop（非删除） |
| watchdog | 四机 crontab watchdog_hardened=1，全程未动 |
|  (node01 管理网末段) head health (8002) | 停机态（正常） |
| 恢复序列 | W9：测试容器清零 → enable --now（三 worker 先、head 后）→ /health=200@8002 → 四 rank healthy 铁证 |

### 1.2 测试现场
| 项 | 状态 |
|---|---|
| w6 测试容器 | 四机全部已清理（NO_W6_CONTAINERS） |
| 测试端口 | 8013（测试）/ 26000（master）/ 生产 8002 未占用 |
| health@8013 | 000（无服务，W3 未达成 ready） |

### 1.3 镜像谱系（核心资产，全部在 REGISTRY_HOST:5000 registry）
| Tag | 镜像 ID | 内容 | 状态 |
|---|---|---|---|
| v0.28.0-sm121a-r3 | b760626fc3fc | 基线：0.28.0 stock+cu132+sm_121 编译件（digest sha256:b760626f...5dec） | 四机在位 |
| sm121a-r4 | 0632dadcde88 | r3+**断点4 守卫补丁**（core.py follower 三行守卫，fork 同款） | 四机在位 |
| sm121a-r5 | bf8cfa62f2eb | r4+**flashinfer#4380 补丁**（main 版 `_sparse_mla_sm120.py`，_DECODE_DSV4_DISPATCH 25 shape 含 (16,192)） | 四机在位 |
| sm121a-r6 | a9a9f4f62d9a | r5+**fa4 包**（flash-attn-4 4.0.0b3，py3-none-any wheel sha256 c5fac755...） | 四机在位 |
| **sm121a-r8（当前最新）** | **5ae19f0b6409** | r6+**fa4 __init__ 惰性化（r7）+vllm __init__ 惰性化（r8）** | **四机在位+已 push（digest sha256:54de247e...760）** |

注：r7=仅 fa4 侧惰性（ (node02 管理网末段) 本地，ID b648a0c015f5，被 r8 包含，无需单独关注）。

### 1.4 r8 验证链证据（交接前实测，2026-08-28 08:45）
```
V1 from vllm.vllm_flash_attn.cute import utils        →（伴随良性 WARNING vllm._C 缺失，历史上同样存在于生产基线，非阻塞）通过
V2 import vllm.models.deepseek_v4.nvidia.ops.fused_indexer_q_cutedsl → V2_MODULE_OK
V3 from flash_attn.cute.cache_utils import get_jit_cache / testing.is_fake_mode → V3_FA4_OK
V4 fa4 version = 4.0.0b3 → V4_VER OK
```

### 1.5 四机 /home/<USER>/w6-kit/ 文件
- 四机共有：`w3_run.sh`（测试容器启动脚本）、`w6_env.txt`（公共 env）
-  (node02 管理网末段) 主库（其余文件按需 scp）：`fa4_lazy_init.py`、`vllm_fa_lazy_init.py`（两个惰性补丁源文件）、`fa4-4.0.0b3-py3-none-any.whl`、`fi-main/`（flashinfer main 源码树）、`w6_patch_guard.py`（断点4 补丁）、各种 probe/diff 取证脚本
- ⚠️ `w3_run.sh` 镜像引用仍 pin **r6 digest（a9a9f4f6...）**，attempt-3 前必须改 r8

---

## 二、W6 窗口至今完整进程账（诚实账）

### 2.1 已完成（T0-T0+73min）
| 阶段 | 内容 | 结果 |
|---|---|---|
| 备料 | Kit 就绪； (node03 管理网末段)/ (node04 管理网末段) 本地副本 sha256 交叉校验 **CROSS_MATCH_ALL_48**（spark01↔ (node03 管理网末段) 48 分片位级一致） | ✅ |
| W1 停机 | 四机 disable+容器停（07:37-07:43） | ✅ |
| W2-断点4 | 根因铁证（0.28 删了 fork core.py:126-129 follower 守卫）→ r4 补丁固化四机 | ✅ |
| W2-flashinfer | PR#4380=dc963cc；0.6.17 不含（反转）；kernel cuh md5 全同、仅分派表缺 shape → r5 修 _sparse_mla_sm120.py | ✅ |
| W2-fa4 | 缺包导致 No module named flash_attn（断点5 初现）→ 装 fa4 4.0.0b3 → r6 | ✅ |
| W3-attempt-1/2 | 失败两次：①启动形态错（ENTRYPOINT=/bin/bash 吞 CMD，改 bash -lc 解决）②fa4×DSL4.6.2 API 漂移 ThrMma（断点5 主案） | 教训已固化 |
| W3-断点5 修复 | r7（fa4 __init__ 惰性）不够→真因 vllm 侧顶层 import 链→r8（vllm __init__ 惰性）→ V1-V4 全过 | ✅ 修复就绪 |

### 2.2 未完成（下阶段任务，按序）
| 阶段 | 内容 | 状态 |
|---|---|---|
| **W3 attempt-3** | w3_run.sh 改 pin r8 → workers 先+head → 轮询至 ready（8013 health=200） | 🔴 立即执行（下节详述） |
| W4 冒烟 | L2/L3+D1-D12 静默降级 grep | 待 W3 |
| W5 性能矩阵 | bench_v2 精简 5 档对标 PR4K=3060 | 待 W4 |
| W6 dspark | 加回 speculative-config E2E（PW6 销案） | 待 W5；见风险 R1 |
| W9 生产恢复 | **红线 15:37 前**，序列见 §1.1 | 必达 |

### 2.3 断点总账
| 断点 | 内容 | 状态 |
|---|---|---|
| ①pynccl | VLLM_DISABLE_PYNCCL=1 规避 | ✅ 关闭（W5-R 已证） |
| ②linear-backend torch | GB10 必死（CC90 门禁），必须 auto | ✅ 关闭 |
| ③flashinfer#4380 | r5 补丁（分派表）+ r6/r8（fa4） | ✅ 供给就绪，E2E 待 W3/W6 实证 |
| ④follower 握手 | r4 三行守卫 | ✅ 补丁就绪，待 attempt-3 日志实证（三机 `Follower node: holding process`） |
| ⑤fa4×DSL4.6.2 | r7+r8 双惰性化 | ✅ V1-V4 过，待集群级实证 |

---

## 三、下一棒执行手册（新团队照此执行）

### 3.1 W3 attempt-3（第一优先，预计 30-45min）
1. 四机改脚本 pin：`w3_run.sh` 中 `<BAKE_IMAGE_DIGEST>` → r8 digest `<BAKE_IMAGE_DIGEST>`（或统一用 tag `sm121a-r8`，四机一致即可）
2. workers 先： (node02 管理网末段) `bash w3_run.sh 1 <NODE_IP> w6rank`； (node04 管理网末段) `... 2 <NODE_IP> w6rank`； (node03 管理网末段) `... 3 <NODE_IP> w6rank`
3. 立即 head： (node01 管理网末段) `bash w3_run.sh 0 <NODE_IP> w6head`
4. 轮询（60-90s 间隔，≤12 轮）：`docker logs --tail 20 <容器>` grep 关键字；重点：
   - 三台 worker 日志出现 **`Follower node: holding process`** = 断点4 补丁生效铁证
   - `Model loading took ... GiB / ...s`（ (node03 管理网末段)/ (node04 管理网末段) 走本地副本应与  (node02 管理网末段) 同级速度）
   - `kv cache block size ... DEEPSEEK_SPARSE_SWA`（KV 阶段=上次死点，过了即突破）
   - `Using 'DEEPGEMM_MXFP4' Mxfp4 MoE backend`（D2 必查）
5. ready 判据： (node01 管理网末段) `timeout 5 curl -s -o /dev/null -w '%{http_code}' http://<NODE_IP>:8013/health` = 200
6. **FAIL 短路**：新类型异常（非已知五断点）即抓 `docker logs ... | tail -60` 停手上报；W3 时间盒 60min，超时上报裁决

### 3.2 W4 冒烟（ready 后 20min）
- 三条生成冒烟（curl /v1/chat/completions，max_tokens=64）
- D1-D12 静默降级 grep 清单（源：`vllm028-kernel-path-audit-2026-08-27.md` §B6）：
  - `grep 'Selected .*LinearKernel'`（linear auto 形态下应见 DeepGemm/FlashInfer 系，无 BlockWiseTorch 属预期——GB10 上 torch 后端不可用）
  - `grep -i marlin|emulation` 必须为 0
  - `grep 'attention backend\|FLASHINFER_MLA_SPARSE'` 必须命中
  - `grep -i 'fallback.*backend|switching'` 必须为 0
- 内存/CPU 巡检：四机 `nvidia-smi`（容器内）mem.used 应 ~100G 量级（0.78 util）

### 3.3 W5 性能矩阵（30-40min，对标生产）
工具：spark01 `<INSTALL_DIR>/bench_v2.py`（requests 已装）；输出 `analyze_bench.py` 归一化。
精简 5 档（对标基线：生产 LuZ0.3.1 PR 4K ~3060 tok/s；旧基线 2744）：
| 档 | 命令要点 |
|---|---|
| DE c1 | `--concurrency 1 --run-type de --rounds 1`（in512/out4096/coding） |
| DE c4 | `--concurrency 4 --run-type de --rounds 1` |
| PR 4K | `--run-type pr --prefix-lens 4096`（若脚本不支持子集即跑 both 全档裁时间） |
| PR 32K | 同上 |
| PR 131K | 同上 |
- endpoint `http://<NODE_IP>:8013/v1`（无 api-key 时去掉 --key）
- 判读规程：全部输出 **% 差值+排名结论**；PR@4K ≥2744（-10%）即视为可继续，<2400 触发裁决
- dspark 指标：/metrics 的 spec_decode counters（本阶段无 dspark，仅采集基线）

### 3.4 W6 dspark（条件执行，60min 时间盒）
- 前置裁决项（风险 R1）：vllm interface 路径已惰性化，dspark 运行时若真调 fa4 `flash_attn_func` 会在运行时炸 ThrMma。**先小步验证**：直接在 r8 容器内调 `flash_attn_func` 小 tensor 试一次；炸则 dspark 段顺延至 fa4/DSL 版本对齐（不阻塞主线），记录即止
- CLI 增补：`--speculative-config '{"method":"dspark","num_speculative_tokens":7,"draft_sample_method":"probabilistic"}'`，重启四容器后复测 DE c1 对比无 dspark 值

### 3.5 W9 生产恢复（**红线 15:37**，预留 60min）
1. 四机 `docker rm -f w6head w6rank` 清零测试容器
2.  (node03 管理网末段)/ (node04 管理网末段) 挂载回滚：`sudo ln -sfn /data/models/deepseek-v4-flash-0731 <INSTALL_DIR>/models/deepseek-v4-flash-0731`（symlink 指回 NFS，用户定位：本地副本仅测试用）
3. 三 worker `echo pw | sudo -S systemctl enable --now vllm-tp4-worker` → head `... vllm-tp4-head`（**顺序不可反**）
4. 铁证清单（全部必采，缺一不可宣告完成）：
   - 四机 `docker ps`：vllm-tp4-rank{0..3} 全 healthy
   -  (node01 管理网末段) `curl http://<NODE_IP>:8002/health`=200；/v1/models=Unauthorized（api-key 门禁在位）
   - 四机 crontab watchdog=1
   - 测试容器=0、w6-kit 保留（资产）
5. 数据固化：w6-logs 四机日志归档 + bench 结果 → 主理人出 W6 终局报告

---

## 四、标准要求与纪律（新团队必须遵守）

### 4.1 组织纪律（人类负责人明确要求）
1. **团队构成必须真正启用 EngineeringAssuranceTeam 专家成员**（architect / sre-engineer / testing-expert / tech-writer / code-reviewer），按角色派发；**严禁通用子代理承载专业工作、严禁主理人亲手执行**
2. 主理人职能边界：裁决、派发、验收、对用户汇报；执行一律经专家成员
3. 每项工作**必须带超时防挂起**：SSH `timeout 25`+ConnectTimeout=6；容器命令 timeout 包裹；禁止 >90s 长等，改 60-90s 短轮询；阶段出数即报（简报 ≤15 行）
4. FAIL 短路：预置 FAIL 条件，命中即停即报，禁止循环重试与猜测性修复

### 4.2 工程纪律
- 生产绝对不动（窗口内 disabled 是正常态；恢复仅走 W9 序列）
- sudo `echo <pw> | sudo -S`，任何输出不得含明文密码
- 改动留 .bak；镜像变更一律 commit+tag+push+四机 digest pin 一致
- 测试容器一律 w6 前缀；端口 8013/26000；生产 8002 永不占用
- 诚实账：未达成项如实标注，禁止粉饰；性能对比必须 % 差值+排名
- 内存红线：GB10 统一内存，OOM=整机冻结 SEV1；容器内存闸不拆

### 4.3 已登记风险与未决项
| # | 风险/未决 | 处置 |
|---|---|---|
| R1 | dspark 运行时或触发 fa4 interface→ThrMma（惰性化只救了 import 链） | W6 前小步试测裁决；炸则 dspark 顺延，不阻塞 |
| R2 | vllm._C 缺失 WARNING（r8 验证 V1 伴随出现） | 生产基线同款良性告警，观察即可 |
| R3 | w3_run.sh 四机 pin 仍为 r6 | attempt-3 第一步必须改 |
| R4 | attempt-2/3 起 head 时机 | 严格 workers 先、head 后（脚本顺序已固化） |
| R5 | max-model-len=32768 冒烟口径 | 生产 600000 的性能对齐留 W5 后视余量裁决 |
| R6 | nvfp4 权重在  (node03 管理网末段)/ (node04 管理网末段) 仍 NFS | W7 nvfp4 段若执行需接受 ~60min 加载或窗口前夜补拷 |
| R7 | flashinfer b12x JIT 冷启时长未测 | nvfp4 段顺带计时（PW7） |

---

## 五、资料索引（deliverables/engineering-assurance/）

| 文件 | 内容 |
|---|---|
| vllm028-w6-window-battleplan-2026-08-28.md | W6 作战计划主文档（时间轴/矩阵/O1-O7 优化清单） |
| vllm028-w5r-final-2026-08-27.md | W5-R 终局（四断点初判，断点④④铁证） |
| vllm028-w5r-window-execution-2026-08-27.md | W5-R 执行细账（恢复序列模板 §7） |
| vllm028-kernel-path-audit-2026-08-27.md | 双权重 kernel 审计（D1-D12 降级 grep 清单 §B6） |
| vllm028-migration-plan-2026-08-27.md | 配置迁移计划（40 项矩阵） |
| vllm028-verify-matrix-sre-2026-08-27.md | r2 验证矩阵 |
| vllm028-precheck-qa-2026-08-27.md | QA Gate Q1-Q7 |
| .window-tmp/w6_patch_guard.py | 断点4 守卫补丁源 |
| 集群  (node02 管理网末段):/home/<USER>/w6-kit/ | 全部 Kit 与补丁源文件（vllm_fa_lazy_init.py / fa4_lazy_init.py / w3_run.sh / w6_env.txt 等） |

## 六、一页速览（贴给新会话的第一条消息建议）

> 接手 W6 窗口（vLLM 0.28 四机 TP4 升级测试，T0=2026-08-28 07:37，红线 15:37 前恢复生产）。当前：生产四机正常停机态（disabled+容器 stopped+watchdog 在位），测试现场干净（无 w6 容器），镜像 r8（含断点4 守卫+flashinfer#4380 分派表+fa4 双惰性化）已四机就位且 V1-V4 验证全过。任务：① w3_run.sh pin 改 r8 → attempt-3 起集群（workers 先 head 后）→ ready(8013 health=200)，三机日志必须出现 Follower node: holding process；② W4 冒烟 D1-D12；③ W5 bench_v2 五档对标 PR4K=3060（% 差值+排名）；④ W6 dspark（先过 R1 裁决）；⑤ W9 生产恢复铁证五件套。纪律：真专家成员分工（SRE/测试/架构/写作），主理人只裁决派发验收；全程 timeout 防挂起；FAIL 短路；生产绝对不动；诚实账。详见 HANDOFF 文档 §3 执行手册。

——交接包完。剩窗口 ~6.7h，红线 15:37。
