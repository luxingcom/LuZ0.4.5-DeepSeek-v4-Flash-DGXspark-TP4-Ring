# G1R7-VL baked5 交付报告（DSpark draft 优化镜像 + 窗口就绪包）

日期：2026-09-03 ｜ 执行时段：其他团队测试进行中——**零容器重启、零 active 文件触碰、零 w6_env.txt 变更**

## 1. 交付物

| 交付物 | 标识 | 状态 |
|---|---|---|
| baked5 镜像 | `LuZ0.4.5-…-TP4-Ring-VL-baked5`（manifest-list digest `<INDEX_DIGEST>…`，config `<CONFIG_DIGEST>`，FROM baked4/<BASE_IMAGE_DIGEST>） | 已推 registry + 四机预拉 + worker 内容 md5 复核一致 |
| 脚本变体 `.w2b5-20260903`（head+worker） | R5=baked5 + inline `-e VLLM_DSPARK_MARKOV_REPL=1`（自包含，不碰 w6_env.txt） | 四机 md5 一致 |
| 脚本变体 `.e1a-20260903`（head+worker） | vision 权重（baked4）+ **无 speculative-config** | 四机 md5 一致 |
| 脚本变体 `.e1b-20260903`（head+worker） | 0731 权重（Ring-baked）+ **无 speculative-config**（基于其他团队 00:32 最新 live 脚本派生） | 四机 md5 一致 |
| 构建上下文 | `g1r7-build/vl-baked5-ctx/`（Dockerfile + 3 补丁文件 + md5 锁） | 门禁内嵌 |

## 2. baked5 内容（三补丁，全部带 env 门控/回退）

1. **C1 Markov 头双复制**（`VLLM_DSPARK_MARKOV_REPL=1` 开）：w1→`nn.Embedding`、w2→`ReplicatedLinear` 全量每 rank，`bias()` 本地 fp32 计算（对齐 logits_processor 累积精度）——消采样循环 12 次串行跨机通信。位级中性已在真实权重离线证明（offline_validate.py，torch.equal True）。补丁文件：`qwen3_dspark.py`（DSparkMarkovHead + ReplicatedLinear 导入）、`dspark.py:145`（replicate 入口）。默认关。
2. **C2 draft topk 封顶**（`VLLM_DSPARK_DRAFT_TOPK=N`，默认 0=关）：`dspark.py` __init__ 三处联动覆盖（`router.top_k` + `dataclasses.replace(moe_config, experts_per_token, routing_method)` + `_set_moe_config` 传播 + `ffn.n_activated_experts`）。离线判高风险（topk4 移除 32% 尾部质量），仅作 A/B 臂。
3. **SKIP_MTP_COPY 条件修复**（无条件生效，`=0` 可回退）：`model.py:1170` `!= '0'` → `== '0'`——默认跳过死拷贝（prefill 满批 268MB/步，TTFT 项），恢复 G1r3-D 注释原意。

## 3. 构建门（全部通过，内嵌 Dockerfile）

py_compile ×3 ｜ 补丁后 md5 断言（q3 `9f170d3f` / ds `ab111bc9` / md `cf0988de`）｜ 标记 grep（markov_replicated/ReplicatedLinear/两 env 名/_set_moe_config/`== '0'`）｜ 负门（旧反转条件必须不存在）｜ 语义抽查（复制默认关、topk 默认 0）。

## 4. 现场纪律确认

- active 文件未动：live `start_tp4_head_v043.sh`（其他团队 00:32 版本，md5 8cab7652）、`w6_env.txt`（无 DSPARK 注入）、systemd 单元、容器（rank0 保持他人留下的 Exited 状态、`wizardly_hoover` Created 未碰）。
- 所有动作 = docker build/push/pull + 新文件创建/scp（CPU 轻 workload）。
- 回退链：baked5 任何异常 → R5 指回 baked4 + guard 重启；C1/C2 行为回退只需去掉 inline env；SKIP 回退 `VLLM_DSPARK_SKIP_MTP_COPY=0`。

## 5. 窗口 runbook（就绪态，待排期；全部走 w9r4_restart_guard.sh）

| 窗 | 臂 | 动作 | 门 |
|---|---|---|---|
| 0 | E1 | `.e1b`（0731 spec-off）→ 探针 → `.e1a`（vision spec-off）→ 探针 | Δ≥6ms=目标侧定案；Δ≤3ms=转 E5；预注册：E1b ≈41-43ms |
| 1 | baked5 | `.w2b5`（vision k=6 + C1 开）vs baked4 基线 65.5 | decode 探针 ≥65.5、accept 画像可比（C1 中性预期逐位同）、GSM8K10 哨兵、boot 0 异常 |
| 2 | k=9 | `.w2k9-20260902`（已就绪） | 净负则回 k=6 |
| 3（备选） | C2 | baked5 + `VLLM_DSPARK_DRAFT_TOPK=5`（加 env 重启或改变体） | 吞吐净增才保（预期负） |
| — | 观察 | logdump 随窗启停；#4802 合入后另排 flashinfer overlay A/B | — |

## 6. 风险与注意

- C1 复制版显存 +132MB/rank（w1+w2 bf16 全量），4×116GB 无压力。
- C2 若启用：首启 b12x autotune 对新 topk 形状重调（warmup 时间增加）。
- SKIP 修复的唯一理论风险 = 某 propose 路径 aux_hidden_states 为 None 时读旧 buffer——G1r3-D 已证 DSpark 恒走 aux 路径；回退开关在。
- 本轮未含 #4802 overlay（未合入上游；单独通道验证）与 #50424 量化头（上游件，后续臂）。
