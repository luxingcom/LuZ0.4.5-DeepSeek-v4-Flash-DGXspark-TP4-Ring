# G1R7-VL W2 报告 — 视觉切换完成（含三层故障修复链）

日期：2026-09-02 ｜ 生产镜像：`…-TP4-Ring-VL-baked4`（digest <BASE_IMAGE_DIGEST>，FROM 链 baked←baked2←baked3←baked4）
服务形态：vision-exp 权重 + k=6 + mmlen 65536 + limit-mm 4 图 + served-model-name `deepseek-v4-flash-vision-exp`
当前状态：**health=200，四机 healthy，W2 三门全绿**

---

## 1. 并窗切换与三轮故障修复链

并窗（-VL-baked + W2 视觉参数一次 guard 重启）按批准执行，触发三个串联故障，逐层定位修复：

| # | 故障 | 根因 | 修复 | 载体 |
|---|---|---|---|---|
| F1 | boot 即崩（spec 校验） | fork DSpark 硬校验：k≥`dspark_block_size(5)` 且 k%`n_predict(3)`==0；PLAN-V2 的 k=3 沿自上游 pinned 镜像（GB200 block 语义不同） | **k=6**（≥5 且 3|6 的最小解；0731 的 k=7 在 n_predict=1 下合法） | 脚本（已固化） |
| F2 | warmup 死于 `Unsupported sparse-MLA prefill: topk=512 topk_extra=512` | SM121 flashinfer `sparse_mla_sm120_prefill.cu` dual 路径**只编译了 SWA 段 TK=128 模板**；vision 图像行 SWA 段=窗口128+图宽384=512 无实例（0731 SWA 恒 128 故 W1 通过）。非移植 bug，是 GB10 kernel 支持面缺口（调研预言的 #4598 同族） | JIT 源码 dual 分支加 **TK=512 FP8 模板实例**；单机 GPU 三层验证（JIT 编译过/dispatch 过/零 KV→零输出数值对） | baked2 |
| F3 | 图像请求 400 `Failed to apply partial…images` → 修复后 500 `KeyError: input_ids` | fork 与 main 的 `BaseMultiModalProcessor` 语义分歧：fork 主链走 `call_hf_processor_mm_only`（要求 HF ProcessorMixin 协议）+ text_mm 路径要求 processor 返回 `input_ids`；PR stand-in `DeepseekV4VLProcessor` 只产图像字段 | `DeepseekV4VLMultiModalProcessor` 覆写 `_call_hf_processor`：走全量 text+mm 调用路径（fork L1229 逃生口）+ 补注入 tokenizer 编码的 `input_ids`（占位符 129264 单 token，PromptReplacement 照常生效）。离线全链验证（prompt_ids+五键 BatchFeature） | baked3/4 |

镜像链：baked（autotune 内置）→ baked2（+kernel TK=512）→ baked3（+processor 路径）→ **baked4**（+input_ids 注入）。每层 Dockerfile 纪律 + md5 断言验证门 + 四机预拉 + 脚本 md5 一致（worker f5afd2c3）。

## 2. W2 功能门结果（baked4）

| 门 | 结果 |
|---|---|
| ① GSM8K 10 题 ×2（baked2/baked4） | **10/10 + 10/10**（vision-exp 权重文本 sanity） |
| ② 单图（红底白方块数字42） | **「42 红色」** PASS |
| ② 4 图（红/蓝/绿/黄） | **「红色，蓝色，绿色，黄色」** 顺序全对 PASS |
| ② span 原子性（3085 tok 长文 + 中插 2 图） | **「红底99，蓝底13」** PASS —— 图像 span 双向注意力在长上下文中正确工作（disable_chunked_mm_input 生效） |
| ③ 日志核验 | 08:04 后 **0 异常行** |
| ③ spec 指标 | 均值接受长度 3.9–5.3，位置接受率 0.92/0.77/0.70 形态健康 |

boot 哨兵（baked2/4 两次验证）：`Resolved architecture: DeepseekV4ForConditionalGeneration`、`Forcing --disable_chunked_mm_input`、DSpark draft **105 params**（3 层 MTP 链自洽：97→102→105 对应 1 层旧码→0731→vision-3层）、autotune **30 configs**（vision 形状新 tune 6 个，#4598 autotuner 正常）、PIECEWISE 16/16 + FULL 11/11 + dspark 图捕获。

TTFT 观察：单图 13.3s（含首启 JIT/缓存冷）、4 图 5.6s、span(3085tok+2图) 11.5s。

## 3. 观察项与 W3 输入

- **decode 单流 65.5 tok/s vs 0731-k7 的 90.6（-28%）**：k=7→6 + 3 层 MTP 链 draft 开销 + vision-exp 权重差异的混合效应。**W3 k sweep（候选 6/9）+ draft 结构调优**的主输入。
- OCRBench 子集未跑（本地无数据集；合成图功能门已覆盖通路正确性，质量基准待数据集就绪）。
- 生产 API 8001 侧调用方需将模型名切至 `deepseek-v4-flash-vision-exp`（ops/调用方协办）。
- window_restart TCPStore 超时仍起 worker 的缺陷在本轮再次复现（07:05 链），已两次记录移交。

## 4. 过程资产

- 修复源：`~/w6-kit/g1r7-build/vl-fix/sparse_mla_sm120_prefill.cu`（+TK=512）、`work/…/mm_preprocess.py`（_call_hf_processor 覆写）
- 取证：logdump（本次启用，按纪律已关闭）、`/opt/_PH_INSTALL_/backup/crash-dumps/crash-20260902-071053-*`、`w6-logs/g1r7vl-w2{,b,c,d}-20260902.log`、`w6-logs/G1R7VL_W2/`
- 复现/验证脚本：`repro_512.py`（GPU kernel 三层验证）、离线 processor 全链脚本（报告内嵌于会话）

## 5. W3 计划

1. k sweep：k=6 vs k=9（DE c1 + 图像基准 + accept 形态）
2. OCRBench/图像质量基准（数据集就绪后）
3. draft 链开销剖析（105 params 3 层 vs 0731 单层的 decode 摊销）
