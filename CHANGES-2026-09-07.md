# CHANGES — 2026-09-07（V5b 修复批次）

V5b（`LuZ0.4.5-DeepSeek-v4-Flash-DGXspark-TP4-Ring-V5b`）为 LuZ0.4.5 之上叠加 2026-09-07 停机窗口（B窗1）全部变更的修复批次。窗口内三次重启失败的根因均定位并闭环（详见 docs/04-issues/）。

## 修复清单（A1/A2/A3）

### A1 — SKIP_MTP_COPY 条件反转修复（bug fix）
- 位置：`model.py` MTP 路径（补丁生成器 `patches/v5b-fix-batch-2026-09-07/apply_v5b_patches.py`）
- 缺陷：环境变量条件写作 `!= '0'`，默认值（未设）时与 `'0'` 不等 → MTP hidden buffer 每步 `copy_` 死拷贝从未被跳过
- 修复：反转为 `== '0'` 语义——默认跳过死拷贝（`SKIP_MTP_COPY` 设 `0` 可恢复拷贝）
- 验证：max_tokens=256 输出完整性 5/5（completion_tokens 全额、token 账目差恒 1）

### A2 — SWA 层 YaRN 缩放泄漏修复（bug fix）
- 位置：`rope.py` compress 层
- 缺陷：SWA（sliding window attention）层被错误施加 YaRN 缩放，长上下文远端信息衰减
- 修复：compress 层仅设 `apply_yarn_scaling=False`，**不触碰 `rope_type`**——首版实现修改了 rope_type 引发 rotary 类血统切换，`fused_inv_rope_fp8_quant.py:189` 的 `cos_sin_cache.dtype == torch.float32` 断言崩溃（Exited(1)），fix2 即规避此路径
- 验证：needle 三档 30K / 128K / 500K（600K 上限 83%）4/4 位置精确复述（见 docs/06-verification/）

### A3 — block_indices 越界 clamp（robustness）
- 位置：`cache_utils.py` block table 索引
- 修复：被污染 lane 的越界索引 clamp 到本请求 block0（in-bounds）；正常路径位级不变（逐行审查证实）
- 可选更稳变体：-1 哨兵 + store mask 排除（本批未采纳，记录于审计文档）

## 环境门控特性（默认关闭）

### B1 — Markov replicate 词表复制（**禁用状态**）
- `VLLM_DSPARK_MARKOV_REPL` 门控，**生产必须保持关闭**：forward 侧未适配（VocabParallelEmbedding 按 `id - vocab_start` 查本地行，全表替换后 rank1-3 查表错位 → 静默数值错乱）；生成器 docstring 声称的 fp32 bias 路径实际不存在
- 防护：生产 env 已固化 `VLLM_DSPARK_MARKOV_REPL=0` 护栏 + 复盘文档 mask 纪律

### B2 — draft router top-k override hook（**未接线**）
- `VLLM_DSPARK_DRAFT_TOPK` 门控；当前版本 hook 仅定义未接线（死代码），旋钮不生效
- 已知 footgun：实现用 `isdigit()` 判定，`"0"` 会通过并置 `top_k=0`——接线时必须加 `k>=1` 下界

## 运维

- **窗口重启守卫 v3**（`scripts/window-restart-guard/w9r4_window_restart.sh`）：flock 互斥（防残留进程抢锁）+ D2 TCPStore fail-fast（120s 超时 → 停 head → exit 2）+ systemd 单元名固定新名（防旧单元 grep 探测误启）+ nohup 后台执行
- 旧 systemd 单元（vllm028-tp4-*）移档 + mask（disabled≠封死）；w6_env 护栏行（MARKOV_REPL=0）四机同步（md5 一致）
- proxy v1 disable（消除重启后 :8001 竞争）；四机镜像收敛至仅 V5/V5b 两 tag
- 告警缺口：healthcheck 连续 FAIL 感知缺失（唯一失效面，SEV2 等效）——待告警闭环

## 验证记录

- GSM8K10 10/10；A2 needle 4/4（30K/128K/500K 全档）；A1 完整性 5/5；SSE 流式回归 3/3（无空帧、finish_reason 正常）
- 三路独立审查（逐行审计 / 行为级 QA / 运维复盘）结论与文档见 `docs/05-kernels-patches/`、`docs/06-verification/`、`docs/04-issues/`

## 脱敏声明

本批次文档与脚本经统一脱敏流程发布：SSH 凭据 / API key / 内网地址 / 主机名 / 操作者账号 → `<PASSWORD>`、`<API_KEY>`、`<USER>`、`<NODE_IP>`、`<INSTALL_DIR>`、`_PH_*_`、`node0X`（语义见 REDACTION-MAP.md）。
