# CHANGES — 2026-09-02（LuZ0.4.5 定版）

LuZ0.4.5-DeepSeek-v4-Flash-DGXspark-TP4-Ring 为基于 LuZ0.4.4（G1r6）的修复定版，叠加 2026-09-02 生产窗口全部变更。

## 生产形态基线（LuZ0.4.5）

- 引擎：vLLM 0.26.1.dev0+gd3d3b2cca（fork 基座，FlashInfer 0.6.18 + B12X MXFP4）
- 量化：W4A4 full（`VLLM_MOE_W4A4=2`）+ CUDA Graph（`VLLM_MOE_W4A4_CG=1`）+ 池补丁（`VLLM_B12X_SHARED_WRAPPER=1`）
- MoE：`--moe-backend flashinfer_b12x`；KV `fp8_ds_mla`；注意力 `FLASHINFER_MLA_SPARSE_DSV4`
- 动态 tile 封顶解除：`VLLM_MOE_DYNAMIC_TILE_CAP=0`（PR4K C1 +2.1% / C4 +0.7%，无崩溃）
- autotune 手动固化：`VLLM_FLASHINFER_AUTOTUNE_CACHE_DIR` 固定路径缓存（跨重启命中 `Loaded 24 configs`，消除 ±2-4% 环境噪声）
- 投机解码：DSpark MTP n=7（probabilistic）
- 参数：max-model-len 600000 / max-num-seqs 12 / batched-tokens 4096 / prefill-threshold 2048 / util 0.80 / CUDA Graph 十六档（1..96）FULL_AND_PIECEWISE
- 工具调用：`--enable-auto-tool-choice --tool-call-parser deepseek_v4 --reasoning-parser deepseek_v4`
- GPU 频率：2400MHz 持久化（gb10-clock-cap.service，四机）

## 变更清单（2026-09-02）

### 性能与平台
- G1r5 全量重测通过：DE C1=99.72、PR4K C1=2671/C2=2061/C4=900/C8=541（2400MHz 制）、GSM8K=0.9363（≥0.930 门）
- G1r6 三窗口：W1 平台等价坐实（autotune 漂移归因）；**W2 TILE_CAP=0 采纳**（C1 +2.1%）；W3 B1（bf16 draft logits）GSM8K=0.9318 < 0.9356 验收门 → **B1 降级为可选参数**（默认关闭，G1r7 候选）
- autotune 手动固化补丁（`flashinfer_autotune_cache.py`）：设 `VLLM_FLASHINFER_AUTOTUNE_CACHE_DIR` 时跳过 hash 子目录、固定路径写 config——跨重启 kernel 选择确定

### 工具调用修复（W9R13）
- `deepseek_v4_encoding.py::encode_arguments_to_dsml()` 防御修复：多用户 agent 工具调用发送空串/null/非法 JSON arguments 时旧实现崩溃（JSONDecodeError/AttributeError）→ 现兜底 `{"arguments": 原值}` 不崩溃（对齐官方 encoding_dsv4.py refs/pr/38）
- 修复已烘焙入 LuZ0.4.5 定版镜像

### 可靠性/自恢复
- 热安全解除：GPU 频率 2300→2400MHz（四机持久化）
- 超时守卫 5 项：活性探针日志推进 300s 判定 / health-cmd `-m 30`+`--health-timeout 35s` / systemd `TimeoutStopSec=300` / monitor CUDA 图首运行预热（health=200 后 warmup 请求触发全链 JIT）/ 8002 存活判定 + failcnt 2 + 冷却窗 1800s
- **GPU 利用率旁证关闭（W9R12）**：nvidia-smi utilization 旁证（≥50%=BUSY）准确性低，已移除——恢复纯探针/failcnt 判定；长上下文防误杀由「日志活性 + 8002 存活 + failcnt 2 + 冷却窗」承担
- healthcheck-rebuild 重建容器名统一（`vllm028-tp4-rank*`）

### 运维
- 四机统一脚本与镜像 tag（LuZ0.4.5-Ring），md5 校验一致
- 自愈链：monitor（head）/ healthcheck timer / watchdog 网关自愈 / gb10-clock-cap 全量持久化

### 检查点内置与脱敏分发（W9R14）
- **autotune 补丁内置镜像**：`flashinfer_autotune_cache.py` 修复版（md5 `702f8f56`）从"挂载 patches 覆盖"改为烘焙进检查点镜像（`-baked` tag）——消除对主机私有 patches 文件的依赖，可随镜像分发/开源推送
- **配套脚本重写**：start 脚本移除 autotune 补丁挂载行（内置后挂载会覆盖修复版）；`VLLM_FLASHINFER_AUTOTUNE_CACHE_DIR` 触发 env 保留
- **镜像脱敏分发**：`-redacted` 脱敏版（清理 pip/uv 缓存、网络配置、tmp 残留；扫描零残留）供网盘分发；镜像不含模型权重（`/models` 运行时挂载）

## 性能指标入口

- 完整基准与最终指标：`docs/02-performance-benchmarks/`、`docs/03-final-metrics/`（benchmark 完成后填充）
