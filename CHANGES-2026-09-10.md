# CHANGES — 2026-09-10（S1 置信门控补丁集 + G4 双臂验收）

本文件记录 2026-09-09 ~ 09-10 生产线的两项主线工作：**S1 置信头接线 + per-request 验证截短补丁集**（默认关门的 env 门控特性）与 **G4 双臂验收终判**（gate-off 性能等价定谳），另含同期的 bug 修复与调查（自愈链冷却窗缺陷、接受率作弊检测护栏）。

## 生产形态基线（本轮）

- 引擎/模型/量化/投机：与 [`CHANGES-VL-2026-09-09.md`](CHANGES-VL-2026-09-09.md) 完全一致（k=6 DSpark MTP probabilistic、W4A4 full、TILE_CAP=0）
- 参数档：max-model-len 600000 / max-num-seqs 12 / max-num-batched-tokens 4096（红线勿动）/ util 0.80
- **本轮唯一生产变更 = 镜像内并入 S1 主体 4 补丁**（gate-off 形态），经 G4 双臂验收 PASS 后保留

## 变更清单（2026-09-09 ~ 09-10）

### 参数组合门禁判定（定谳）
- **唯一通过全部门禁的组合 = `batch=4096 × k=6 × thr=0`**（即现役锁定态）
- `batch=4608`：可启动但效率门禁不通过（净利 < 噪声带，KV −8.0%）；`batch=8192`：引擎层 `max_num_tokens=4608` 硬上限拒绝
- `k=3/k=4`：DSpark validator `block_size=5` 硬闸拒绝；`k=5`：硬门禁过但收益 0~+6% < +8% 门槛
- 详见 [`docs/02-performance-benchmarks/gate-combo-verdict-20260910.md`](docs/02-performance-benchmarks/gate-combo-verdict-20260910.md)

### S1 补丁集（默认关门 · env 门控）
- **五补丁**：`dspark.patch`(4 hunks) → `dspark_speculator.patch`(5) → `scheduler_verify.patch`(9) → `w1_glue.patch`(6=2+2+2 三文件联合) → `w5_diag.patch`(6，DIAG 打点，未并入现役镜像 = 设计态)
- **机制**：gate on 时逐步采集 draft 置信分（FULL graph 捕获内零分配零同步）→ 非阻塞 D2H 双缓冲 → scheduler 以 survival=cumprod(conf) 的最长前缀长度做 per-request 验证截短（延迟≠丢失，被截槽位下轮重验）
- **gate off = 控制流 no-op**：`confidence_head=None` ⇒ `_conf_enabled=False` ⇒ 全部 confidence 路径短路，与并入前逐语句等价（G4 bit 级一致的最强保证）
- **补丁序列等价性实测定谳**：序 A（五条顺序套）≡ 序 B（仅 verify→w5），逐字节相同终态（scheduler md5 `1c344c82…`）
- **硬纪律**：`patch -p0 < file` stdin 重定向，绝不可带显式目标文件参数（w1_glue 带参会假 FAIL "2/2 hunks"）
- 详见 [`patches/conf-gate-s1-20260910/`](patches/conf-gate-s1-20260910/README.md)（README + 权威落地序 apply-order.txt + 5 补丁）
- 设计草案与第五补丁独立复审：[`docs/05-kernels-patches/s1-design-draft-20260910.md`](docs/05-kernels-patches/s1-design-draft-20260910.md) / [`docs/05-kernels-patches/w5-diag-review-2026-09-10.md`](docs/05-kernels-patches/w5-diag-review-2026-09-10.md)

### G4 双臂验收 · 终判 PASS（gate-off 性能等价）
- OLD 臂（并入前镜像）5 轮 decode p50 mean **81.9000** / NEW 臂（S1b，含 S1 主体 gate-off）mean **82.3340**
- **Δ = +0.4340 tps (+0.530%)**，|Δ| ≤ F = 4×MAD(OLD) = **4.8800**（臂内噪声地板）；CI95 = [−3.9306, +4.7986] 跨 0
- **裁定：PASS（不可区分）⇒ 保留 S1b 不回退**。gate-off 下性能等价是预期内的正确结果（机制判据 = 控制流等价 + 运行期 env_gate=UNSET + 值守四件套零异常）
- **诚实标注**：mean(+0.53%)/median(−1.18%) 方向相反，判定以 mean 体系为准（median 如实记录不隐藏）；本轮不能评断 S1 闸门收益（gate off ⇒ 收益项未被检验，属 G2 开臂阶段）
- **负结果如实归档**：G4-T1 token 精确比对原理性不可行（臂内自洽率 4.0%/2.0% < 交叉 7.3% ⇒ 服务器对自身不可复现），判据改采契约等价
- 详见 [`docs/06-verification/G4-dualarm-verdict-20260910.md`](docs/06-verification/G4-dualarm-verdict-20260910.md)

### bug 修复 / 缺陷调查（生产保障线）
- **自愈链冷却窗自锁缺陷（查清定谳，修复待窗口）**：`healthcheck-rebuild.sh` 冷却判据 `NOW-LAST` 读到的 state 被人工写入未来值 ⇒ 差值为负 ⇒ 冷却恒真 ⇒ 探针失败也永不重建（自愈重建腿被静默解除武装）；且写入点在重建成功之后 ⇒ 永不自愈，须人工清 state。**修复方案定谳 = 仅 A 一行护栏**（`d=NOW-LAST` 加 `d >= 0` 单侧放行），经逐场景枚举复算验证与 B+A 组合全等效。教训：涉及判据组合必须逐场景枚举复算，不可凭语义直觉。
- **接受率作弊（回顾性接纳）检测护栏**：G2/G3 护栏指标可信性验证——"接受率升高"默认应判可疑而非收益；给出 D1-D5 五判别量 + 一行现场判定规则（三角互洽 / 深位条件率形状 / 边界巧合，最终由贪心序列逐位相等一锤定音）。详见 [`docs/06-verification/accept-rate-cheat-detection-2026-09-10.md`](docs/06-verification/accept-rate-cheat-detection-2026-09-10.md)
- **K 槽位控制实现路径调研**：短期不重建镜像只能测 k=3（方案 A，纯配置）；N=4/5（方案 B）在 fork FULL CUDA Graph 形态下必然撞图，必须重建镜像。为 G2 开臂后的 k 扫描提供代码级判定依据。详见 [`docs/01-research-reports/k-slot-control-implementation-2026-09-09.md`](docs/01-research-reports/k-slot-control-implementation-2026-09-09.md)

### G2 开臂预备（未执行 · 设计就绪）
- A/B 双臂预检设计与开臂就绪度评估已交付（`docs/06-verification/g2-preflight-ab-design-2026-09-10.md` / `g2-open-arm-readiness-2026-09-10.md`）；G2 主窗口将承担 `VLLM_DSPARK_CONF_MIN ∈ {0.1..0.9}` 扫描与 S1 闸门收益检验

## 验证记录摘要

- S1 六目标文件终态 md5 全量回放 6/6 一致（EXP11）；三文件 ast.parse 全过
- G4 双臂各 5 轮，值守四件套零异常（StartedAt 全程未变 / TAG 无漂移 / health=200 / 触发主动重建=0）
- 统计量独立复算 8/8 逐位命中
- 门禁组合判定 12 组合全覆盖（3 batch × 4 k/thr 档）

## 脱敏声明

本文档集由内部工程记录脱敏生成：内网 IP、主机名、SSH 凭据、镜像 RepoDigest 已替换为占位符（`REGISTRY_HOST`、`node0X`、`<BAKE_IMAGE_DIGEST>` 等），规则见 [`REDACTION-MAP.md`](REDACTION-MAP.md)。本轮发布树新增文件全部通过脱敏扫描（敏感模式残留 = 0）。
